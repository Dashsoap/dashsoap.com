# Javascript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#
version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/node

      # Specify service dependencies here if necessary
      # CircleCI maintains a library of pre-built images
      # documented at https://circleci.com/docs/2.0/circleci-images/
      # - image: circleci/mongo:3.4.4

    working_directory: ~/repo

    steps:
      - checkout

      - add_ssh_keys:
          fingerprints:
            - "61:de:11:cd:29:a6:9d:70:b1:f8:ad:bb:ba:3f:54:c1"

      - run: yarn global add vuepress vuepress-theme-meteorlxy

      - run: yarn add -D @vuepress/plugin-google-analytics@next

      - run: npm ci

      - run: echo "Install Vuepress"

      - run: git config --global user.email "360062589@qq.com"

      - run: git config --global user.name "dashsoap"

      - run: yarn deploy

    branches:
      only:
        - dev
